// Inline If with Logical && Operator 
//  condition > 0 mean that if the condition is true then execute the statment 
{condition > 0 && 
    <div>statement</div>
}
//  if the condition is false 
// <div>0<div>  will return

// ====================

// Inline If-Else with Conditional Operator 
//  condition ? true : false.